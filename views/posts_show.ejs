<%- include('partials/header') %>
<%- include('partials/navbar') %>

<div class="container">
    <div class="d-flex justify-content-between align-items-center mb-2">
        <h1 class="h3 m-0"><%= post.title %></h1>
        <div>
            <a href="/posts/edit/<%= post.id %>" class="btn btn-warning btn-sm">Editar</a>
            <form action="/posts/delete/<%= post.id %>" method="POST" class="d-inline" onsubmit="return confirm('¿Eliminar este post?');">
                <button class="btn btn-danger btn-sm" type="submit">Eliminar</button>
            </form>
        </div>
    </div>
    <small class="text-muted d-block mb-3"><%= new Date(post.created_at).toLocaleString() %></small>
    <p style="white-space: pre-wrap;"><%= post.body %></p>
</div>

<%- include('partials/footer') %>
