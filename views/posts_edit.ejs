<%- include('partials/header') %>
<%- include('partials/navbar') %>

<div class="container">
    <h1 class="h3 mb-3">Editar post</h1>

    <form action="/posts/update/<%= post.id %>" method="POST">
        <div class="mb-3">
            <label class="form-label">Título</label>
            <input type="text" name="title" class="form-control" required maxlength="255" value="<%= post.title %>">
        </div>
        <div class="mb-3">
            <label class="form-label">Cuerpo</label>
            <textarea name="body" class="form-control" rows="8" required><%= post.body %></textarea>
        </div>
        <button class="btn btn-primary">Actualizar</button>
        <a href="/posts/<%= post.id %>" class="btn btn-secondary">Cancelar</a>
    </form>
</div>

<%- include('partials/footer') %>
